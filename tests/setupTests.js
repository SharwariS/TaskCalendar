// tests/setupTests.js

import '@testing-library/jest-dom/extend-expect'; // Provides additional matchers like toBeInTheDocument
import { jest } from '@jest/globals';

// If using any global setup or teardown scripts, you can add them here.
// For example, you can configure global mock behaviors or initializations.
